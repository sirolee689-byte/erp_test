# 出库单模块

## 页面范围

- 管理出库单：查询、查看、编辑、审核、反审核、删除、回收站恢复、彻底删除、打印数据入口。
- **列表默认只显示已审核**（`pass='1'`）；打开「显示未审核」或点顶部「审核申请」才看待审核单。
- **列表分页**：默认 **10 条/页**；可选 **10 / 20 / 50 / 100 / 200**（后端上限 200）。
- **列表「出库单数据」列**（出库日期后）：两行汇总，已去掉单独的「数量 / 不含税金额 / 含税金额」列（方案 B）。
  - 第 1 行：`关联单位`（主表 `kehu`）、`货仓`（主表 `ck`）——数值**蓝色**。
  - 第 2 行：`总项数`（蓝）、`含税总价`（紫）、`不含税总价` / `税点总价` / `出库总数量`（深红）；无 `price` 权限时不显示三个价格。
  - 汇总口径：明细 `kcaq01=出库单号` 且 `del=0` 行数；`kcaq051`/`kcaq05`/`kcaq03` 合计；税点 = 含税 − 不含税差额合计（列表 SQL 已聚合）。
- **列表展开明细**（2026-06-24）：主列表最左展开箭头，点击后按需调用 `GET /api/stock-out/:id` 拉明细；也可点行展开（点操作按钮区不触发）。
  - 主列表与入库单一致，使用视口底部固定横向滚动条（`v-erp-list-h-scroll`）。
  - 查询：`UB_ERP_Stocks_out_list` 中 `kcaq01=当前出库单号` 且 `del=0`，按 `seq` 排序；SQL 仅用 `l.*` + `colorName`，**禁止**再 `AS kcaq03` 等同名列（否则接口数值变数组，界面会显示 `0.00` 或 `2,2`）。
  - 列：序号（1、2、3…）、材料编码/名称/规格、颜色、单位、数量、单价、单价（含税）、金额、金额（含税）、税点、末两列（见下）；价格列受 `price` 权限控制。
  - **数量/单价/金额**：展开明细的**数量**最多显示三位小数且不补零（如 `400` 显示 `400`，`3.3999` 显示 `3.4`）；单价、金额仍固定 **两位小数**、不加千分位逗号。
  - **颜色**：主显 `kcaa11`，并按 `kcaa01` 中 `/` 后颜色代码查 `UB_ERP_Stocks_colorcode.code` 得名称，拼接为 `TM(深啡色)` 格式。
  - **备注**：仅显示 `Describe`。
  - **成品出库（类型 6）**末三列标题改为：报关单号（`Reference`）、报关型号（`Describe`）、报关单价（`kcaq08`）；其他类型为 PO/PI、备注（**已去掉唯一码列**）。
  - 表底「小计」行：**「小计：」在数量列左侧（单位列）**；汇总数量、不含税/含税金额；单价列为金额÷数量的加权平均；成品出库的报关单价列小计留空。
- **列表筛选区两行布局**与入库单一致：第一行「供应商/外协商 + 出库类型」，第二行「关键词 + 查询 + 回收站/显示未审核开关 + 重置」；输入框宽度 240 / 160 / 420 px。
- 出库单添加：分为“出库单基础资料”和“出库单明细”两个区域，布局参考入库单。
- **明细工具栏**（2026-06-24）：仅「批量添加」「删除选定明细」「删除全部明细」三按钮，样式与入库单一致（`primary plain` / `danger plain`）；**已移除**关键字搜索框、「增加明细」及关联型提示灰字。
- **明细删除**（2026-06-24）：表最左为「选择」列，点「删除」标记为「已选择」；「删除选定明细」只移除已标记行；「删除全部明细」二次确认后清空；删行仅改当前页内存，保存后才落库。
- **编辑页明细列**（2026-06-24）：顺序为选择 → 序号 → 材料编码/名称/规格/颜色/单位（**只读**）→ 数量 → 单价/单价(含税)/税点/金额/金额(含税)（有 `price` 权限）→ **厂款号/PI号**（可编辑，落库 `UB_ERP_Stocks_out_list.Reference`）→ 备注（`Describe`）；**已去掉「来源明细」列**（`kcaq02` 仍由批量添加/选单带回，保存时写入来源明细键供审核反写）。厂款号/PI号与备注列宽约 30 字（`min-width` 420px）。明细表使用与主列表相同的视口底部横向滚动条（`v-erp-list-h-scroll`）。
- **基础资料行序**（2026-06-24）：①出库单号 ②出库日期 ③出库类型 ④关联单号 ⑤关联单位 ⑥仓库+纸质单号+预留单号（**强制同一行**，三框各 200px、不换行）⑦是否含税 ⑧备注。
- **关联单号按类型**（2026-06-24，2026-06-25 更新，2026-06-26 生产领料选派工）：其他出库可手填（选填）；采购退货为只读 +「选择」+「清空」…；**外协领料**为只读 +「选择」+「清空」…；**生产领料**为只读 +「选择」+「清空」，须**先选生产车间**再点「选择」，否则提示「请先选择生产车间!」；弹窗标题「派工单列表(已选生产车间:xxx)」；选中后回填 `kcap04`（派工单号）、`kcap08`（PI，只读）、`kcap05/kehu`（生产车间，选项格式 `编码,名称,`）、前端隐藏 `sourceSystemcodeId←派工主表 systemcode` 并清空明细；其它关联型仍可手填且必填。
- **生产领料关联派工单选派**（2026-06-26）：接口 `GET /api/stock-out/production-dispatch-source-page`；主表 `UB_ERP_Dispatch_order` 须 `del=0/pass=1/closed=0` 且 `scaj05=当前车间`；排序 `scaj01` 倒序；显示条件默认「只显示派工单号」，可切「全部显示」展开明细（`scak02=GUID`）；关联出库单号按 `UB_ERP_Stocks_out.kcap04` 聚合（已审显示单号 / 未审显示「未审：单号」/ 无单显示「未出单」）；分页 5~200 条/页。
- **生产领料批量添加**（2026-06-26，2026-06-25 列/合并对齐外协）：类型 `4` 选定派工单并带出 PI 后点「批量添加」→ 独立新窗口 `/inventory/daily/stock-out-production-issue-batch-window`；须已选是否含税、仓库、生产车间、关联派工单号、PI 号；车间为空提示「请先选择生产车间!」。
  - **数据来源**：派工明细 `UB_ERP_Dispatch_order_list`（`scak02=GUID`）经 `UB_ERP_Bom_pi_cost` 展开实际出库材料（对齐旧 `ub_erp_stocks_out_s5`）；`sid=PI` 且 `(top_kcaa01=派工物料 OR pq=派工物料)`；展开阶段仍按 pi_cost 全套物料属性分组。**开料部（车间 04）** 改走开料专用逻辑，见上文「开料部生产领料批量」。
  - **同子料合并显示**（2026-06-25）：批量窗口按子料编码 `kcaa01` 聚合，跨派工明细的同编码子料仅显示一行；**需出库数量**按行累加（派工展开需求 `SUM(kcac06×scak03)`）；派工维度与 PI 共用池维度已出各只扣一次。
  - **数量口径**（2026-06-25 更新，对齐旧系统双道限制）：
    - **需出库数量** = 本派工单材料总需求（`SUM(kcac06×scak03)` 合并累加，**不扣**已出库）；
    - **PI 共用池总量** `piDemandQty` = 全 PI `SUM(kcac06×ISNULL(temp,1))`（`pi_cost.sid=PI、isok=1、kcaa01=子料`），跨车间共用；
    - **PI 已出/占用** `piIssuedQty` = 同 PI（`kcap08`）+ 同子料已出库合计（`h.del=0`，**不按**车间/派工/仓库/`pass` 过滤）；
    - **派工剩余** = 需出库 − 本派工单(`kcap04`)+本仓+子料已审/未审出库；
    - **PI 剩余** `piRemainingQty` = PI 总量 − PI 已出；
    - **还需出库数量** = `min(派工剩余, PI剩余)`；
    - 库存三列按子料+当前仓；默认可选/带回 = `min(还需出库, 实际库存)`，三位小数。
  - **列表列**：操作、货品/派工对应、需要领料材料编码（红字）、材料名称（固定约 100px）、规格（固定约 120px，长文本格内换行）、颜色、单位、库存三列、**还需出库数量**（醒目色）、**需出库数量**、**未审出库情况**（三行：未审数量/未审单数量/未审单号）；表格 `table-layout: fixed` 减少横向滚动；已去掉 PI 用量/剩余、最终可领、单价金额列（**列表不展示单价，但保存带回仍按子料 `kcaa01` 取最近已审采购订单明细价写入 `kcaq04/kcaq041/tax`，无采购价写 0**）。
  - **带回**：`kcaq02←首个派工明细键 scak02`、`kcaa01←pi_cost 子料`、`kcaq03←默认可选量`、`kcaq031←实际库存上限`、`kcaq04/kcaq041/tax←子料最近采购价`、`kcaq05/kcaq051←数量×单价`、子料 `kcaa01~35` 快照、`Reference←PI`、**`Describe←UB_ERP_Bom_000.kcaa02`（键=派工物料/`pi_cost.pq`；同子料多货品名称去重后用 ` / ` 拼接，仅名称无其他文案）**；行键 `material|子料kcaa01`；编辑态另按 `*|子料` 兜底去重；另带 `sourceDemandQty`（需出库）、`stillNeedQty`（还需出库）、`piRemainingQty` 等供父页改数量时校验（库表不存）。
  - **父页改数量校验**（2026-06-26）：生产领料明细在「出库单明细」改数量时，上限取 `min(需出库数量, 还需出库数量, PI剩余可领数量, 实际库存)` 中最紧一项；超出则弹窗提示，点「确定」后自动回退到上限并重算金额（与保存时后端 cap 口径一致）。**编辑打开单据**时会按当前派工单/生产车间/PI/仓库调批量接口补齐上限字段（须传 `workshopCode←生产车间 relatedPartyCode`，库表不存这些字段）。
  - **界面配色**：实际材料编码红字；账存红；未审出库紫；实际库存蓝（≤0 红）；还需出库醒目色；可选黄色「选择」/已选灰色。
- **出库类型按钮区**（2026-06-26，2026-06-25 更新）：顶栏保留「管理出库单」「出库单添加」；**「开料出库配置」紧挨在「出库单添加」右侧**（同一行 mode-bar）。**已移除**「搜索出库单」「恢复出库单」「审核申请」「导出信息」（列表区仍有查询、回收站、显示未审核开关）。**超级管理员**（`UB_ERP_User.is_admin=1`）可见「开料出库配置」，维护 `UB_ERP_Stocks_material.cutting_issue` 纳入开料出库的物料分类；登录接口会把 `is_admin` 写入 `localStorage.erp_user`，未重新登录时亦按角色名「系统管理员」或全权限 JSON 兜底显示。新增页隐藏「外协退货」「生产返修」；历史 3/5 单仍可编辑查看。
- **开料出库配置**（2026-06-25）：`GET/PUT /api/stock-out/cutting-issue-config`；仅超级管理员可保存；迁移脚本 `scripts/migrations/sqlserver_stock_out_cutting_issue_flag.txt` 将旧系统五类 `0008/0011/0015/0017/0020` 默认设为开启。
- **开料部生产领料批量**（2026-06-25）：生产车间 **04（开料部）** 时 `GET /api/stock-out/production-issue-batch-lines` 返回 `batchMode=cutting`，**不走**派工明细逐条展开；直接读 `UB_ERP_Bom_pi_cost`（`sid=PI`、`isok=1`），筛选 `t_kcaa01` 或 `top_kcaa01` 为 `CUT-` 裁片结构，且 `kcaa05` 命中 `cutting_issue=1` 的材料分类；出库编码=`pi_cost.kcaa01`；列表**需出库**=`SUM(kcac06×temp)`（裁片子集）；**PI共用池**总量/已出/剩余口径与普通生产领料一致；`Describe←pi_cost.kcaa02`；`kcaq02←CUT|子料编码`（审核不误写派工 `scak04`）；批量窗口「裁片父级」列展示 `t_kcaa01`/`top_kcaa01`。
- **外协领料基础资料**（2026-06-26）：选类型 `2` 时直接进入普通外协领料，不再弹出「是否需要加工后外协？」；基础资料不再显示「加工车间」，保存时也不再带出生产车间 `cj/cjname`。关联单位标签为「外协商」，选项格式 `编码,名称`；**PI号**只读（`kcap08`）；外协领料时隐藏纸质单号/预留单号行。
- **外协领料批量添加**（2026-06-24，2026-06-25 更新，2026-06-24 BOM 子料展开，2026-06-26 订单外发取价修正）：类型 `2` 选定外协单后点「批量添加」→ 独立新窗口 `/inventory/daily/stock-out-assist-issue-batch-window`；须已选仓库、是否含税、外协单号、外协商；PI 为空时按外协单号查询，PI 不为空时叠加 PI 号精确筛选。
  - **列表列**（2026-06-24 列修正）：已去掉 RMB 单价/含税/金额/税点五列，以及返厂编码、实际入库数量列。核心列：库存账存数、物料未审出库数、实际库存（均按**子料 kcaa01 + 当前仓库**，页面**固定三位小数**显示）；**需出库数量、还需出库数量**为**最多三位小数、整数不补零**（与父页出库数量展示一致，区别于库存三列的固定补零）；还需出库数量 = 来源需求（换算外协数量×单用量）− 本单号+本仓+子料已审出库 − 未审出库；默认可选数量 = min(实际库存, 还需出库)；外协数量列显示换算后 `ksum(wxak03)`；实际出库数量 = 来源已审出库；**未审出库情况**在批量窗口内三行展示（未审数量 / 未审单数量 / 未审单号，仅前端展示模板）；是否存在转换数据按子料单位显示。
  - **同子料合并显示**（2026-06-25）：批量窗口按子料编码 `kcaa01` 聚合，跨来源明细/外发对应的同编码子料仅显示一行；“还需出库数量”等需求口径字段按行累加（示例：`0.3 + 2 = 2.3`）。
  - **查询条件**（2026-06-25）：关键字**仅支持子料材料编码**模糊匹配。
  - **子料展开（旧系统口径）**：
    - **订单外协**（外协单 `wxaj03=1`）等：外协明细 `kcaa01` 为「外发对应编码」；带 PI 时按 `sid=PI` + `pq=款号(Product)` 读 `UB_ERP_Bom_pi_cost`；**子料候选**以 `UB_ERP_Bom_000.systemcode` 下 `UB_ERP_Bom_parts` **直接子层**为准（`wxak02` 仅兜底）。单用量：`top_kcaa01=外发对应` 时按 pi_cost 汇总 `kcac04`；BAG 锚点 PI 则用 Bom_parts 的 `kcac04`；无子料才兜底外协明细本身。
    - **订单外发**（外协单 `wxaj03=2`）：**不**走 Bom_parts；按 `UB_ERP_Bom_pi_cost.sid=外协明细.pi`、`pq=外协明细.kcaa01`（外发对应编码）查实际出库子料；同套物料属性与来源字段一致时 `SUM(kcac06)` 为单用量；列表红字编码 = `pi_cost.kcaa01`，外发对应 = 外协明细 `kcaa01`；带回/落库 `kcaa01~kcaa35` 等快照取自 pi_cost 行；`kcaq02` 仍为外协明细 `wxak02`；出库明细单价/含税单价/税点按子料 `kcaa01` 从最近一条已审核采购订单明细取 `kcak04/kcak041/tax`，无采购价时写 0，不再使用外协明细单价或 BOM 销售价。
  - **列表列**：「需要外协材料编码」列内多行展示：子料编码（红字）、外发对应、外协数量、单用量；外协数量整数不显示 `.00`（已去掉独立「外发对应」列）。
  - **数量精度对齐**（2026-06-25）：接口 `sourceDemandQty` / `stillNeedQty` / `issueableQty`、`kcaq03`、`availableQty` 与列表展示**统一三位小数**；修复「界面显示 186.667、保存报 cap 186.6666」的精度不一致问题。
  - **数量口径调整**（2026-06-25）：**需出库数量** = 来源总需求（`sourceDemandQty` 按子料合并累加），**不扣**已出库，始终显示外协单对该子料的总需求量（示例：`LA-0368/BO` 固定 **1950.249**）；**还需出库数量** = 来源总需求 − 本外协单+本仓+子料已审出库 − 未审出库，同子料合并时**已出库只扣一次**（示例：出 50 后 **1900.249**）；“需要外协材料编码”区块内的“外协数量”为整张外协单外协数量汇总（示例：`50 + 100 = 150`）。
  - **PI 提示**：仅无展开或全部兜底 `self` 时提示；Bom 子层已成功展开不弹误导蓝条。
  - **需要外协材料编码列**：首行子料编码（红字）；下方依次为外发对应（青绿辅助色）、外协数量、单用量；名称规格颜色等取**子料**资料。
  - **可选判断**：实际库存 > 0 且还需出库 > 0 时显示黄色「选择」；库存不足或来源已出满不可选；**父页已有明细或编辑态本单已落库明细**（去重键 `wxak02|子料kcaa01`；编辑态另按子料编码 `*|kcaa01` 兜底，避免历史 `kcaq02` 与 BOM 展开 `wxak02` 不一致时仍重复勾选）显示灰色「已选择」且不可再点。编辑态若先在父页删掉明细但未保存，重新打开批量添加时以父页当前明细为准，可重新选择被删行。
  - 选行去重键 `wxak02|子料kcaa01`；父页打开批量窗口时传 `currentLineKeys`（含 `*|子料` 兜底键）；编辑态另传 `excludeOutboundNo`，后端会读本单 `UB_ERP_Stocks_out_list` 已存明细键与子料编码合并去重；带回 `kcaq02←wxak02`、`kcaa01←子料`、`kcaq03←默认可选量`、`kcaq031←实际库存上限`、子料资料快照、PO/PI、备注（外协明细 Describe/info）。
  - 选单弹窗 `GET /api/stock-out/assist-issue-source-page`：仅显示来源剩余 > 0 的外协明细；主表须 `del=0/pass=1/closed=0`，明细须 `del=0/pass=1`；入库数量按外协单+明细键汇总已审外协入库；外协商下拉 `s_lb IN (外协,共用)`。
  - **外协选单性能（2026-06-25，方案 A）**：列表单次 SQL（`COUNT(1) OVER` 取总数，不再默认双查 count+list）；入库数量改 `inbound_agg` 预聚合 `LEFT JOIN`（不再逐行关联子查询）；主从 JOIN 直比 `l.wxak01 = h.wxaj01`。
- **其他出库关联单位**（2026-06-24）：基础资料「关联单位」为**自动完成**（`el-autocomplete`）：可下拉选销售客户（`UB_ERP_System_sales_customer`，`del=0` 且 `pass=1`），也可**手填文本**且不会被自动清空。选客户时选项显示 `客户编码,客户名称`，保存 `kcap05`←`s_code`、`kehu`←`s_name`；手填时仅写 `kehu`，`kcap05` 留空；**选填**。
- **出库类型按钮**与入库单一致：`size="large"` + `stock-type-btn`（高 42px、字号 16px、间距 10px）。
- **表单无「返回列表」**：回列表用顶部「管理出库单」；保留「重置」「保存」。
- **出库日期**新增默认本机当前时间（与入库单 `nowText` 一致，不用 UTC `toISOString`）。
- **纸质单号 / 预留单号**：分别落库 `kcap08` / `kcap09`（原 `kcap08` 上的 PI/PO 类旧数据会显示在纸质单号）。
- **新增默认仓库**：点「出库单添加」或「重置」时，若仓库为空，自动选中名称或编码为「货仓」的仓库（与入库单一致）；仍可手工改。
- **其他出库批量选材**（2026-06-24）：类型 `0` 点「批量添加」→ 独立新窗口 `/inventory/daily/stock-out-other-batch-window`；须已选仓库与是否含税；打开时校验父页仓库 `kcap06`，不一致提示「仓库数据错误，请检查所选仓库」并关窗。
  - 数据：已审核未删入库明细按 **`kcaa01` 分组**；账存 = 本仓已审入库 − 本仓已审出库；未审出库合计；实际库存 = 账存 − 未审；仅实际库存 > 0 可选。
  - 分页默认 **5** 条/页，可选 5/10/25/50/100/200；空结果「查询结果：没有查询到相关信息」。
  - 固定列：操作、产地、材料编码、账存（红）、未审出库、实际库存（蓝；≤0 红显 0）；扩展列：名称中/英、开票名、规格、单位、分类。
  - 选料带回：数量 `kcaq03`/`kcaq031` 默认实际库存；单价/含税单价/税点优先取该物料本仓最近一条「入库主表已审核且 `sp_flag=1`」的入库明细价，无则 0。
  - **性能（2026-06-24，方案 A）**：列表单次 SQL（`COUNT(1) OVER` 取总数，不再默认双查 count+list）；库存汇总 JOIN/WHERE 直比字段；取价改为 `IN` + `ROW_NUMBER` 批量一条 SQL（不再按物料循环查库）。
  - 彻底删除超级管理员：后端按 `UB_ERP_User.is_admin` 实时查库（与入库单一致），不依赖登录令牌缓存。
- **采购退货批量添加**（2026-06-24）：类型 `1` 在选定采购单后，点「批量添加」→ 独立新窗口 `/inventory/daily/stock-out-purchase-return-batch-window`；须已选仓库、是否含税、采购单号、供应商。
  - 可退数量口径：本仓该采购明细已审入库（`Storage.kcan04+kcao02+kcan06`）− 已审退货出库 − 未审退货出库占用；再与仓库实际库存（账存−未审出库，按 `kcaa01`）取小。
  - 列表展示采购单全部有效明细；仅「当前可退数量」> 0 可选；选行带回 `kcaq02←kcak02`、`kcaa01~35`、`kcaa26/kcaa27`（审核回写 `kcak07` 单位换算）、价格税点、PO/PI、备注；数量默认可退数量。
  - 编辑态传 `excludeOutboundNo` 排除本单未审占用；去重键为采购明细 `kcak02`。
- 第一版开放类型：0 其他出库、1 采购退货、2 外协领料、3 外协退货、4 生产领料、5 生产返修、6 成品出库、7 生产领料计划外、8 生产领料补数、9 盘亏出库、10 销售出库。
- **列表状态列**仅显示「已审核 / 待审核」，不再单独展示「只读类型」等标签。
- **不做财务复核**：出库主表 `UB_ERP_Stocks_out` 无 `sp_flag` 字段，界面与接口均不提供复核入口。

## 关键业务规则

- 新增默认未审核，系统不支持自动审核。
- 出库单号 `kcap01` 在保存时生成，前端只展示，不允许手工改。
- **保存落库快照**（2026-06-24）：主表 `uid/uname/utruename/addtime` 写入当前登录人；明细 `kcaa02_en` 抄 BOM 英文名称，`uid/uname/utruename/addtime` 同样写入当前操作员（与入库单明细一致）。
- 明细不能任意手填物料，必须来自库存或来源单据选择；**不再提供「增加明细」手加空行**。
- **出库数量精度**（2026-06-26）：明细 `kcaq03` 按三位小数处理；手工输入、批量添加带回、保存提交前都会规整到三位。展开明细显示时不强制补零，整数仍显示为整数。
- **草稿可空明细保存**（2026-06-25）：未审核出库单可先只保存单头、明细为空；**审核时**后端校验须至少一条有效明细（`del=0`），否则提示「出库单至少需要一条明细才能审核」。
- 未审核出库占用可用库存；审核后变成正式出库；反审核后重新回到未审核占用。
- 已结案旧数据仍禁止编辑、审核、反审核、删除（操作列可能显示「只读」）；界面状态列不单独展示结案标签。
- 审核/反审核会同步来源明细已出数量：
  - 采购退货：`UB_ERP_Buy_order_list.kcak07`
  - 外协领料/外协退货：`UB_ERP_assist_order_list.wxak08`
  - 生产领料：`UB_ERP_Dispatch_order_list.scak04`
  - 生产返修：`UB_ERP_Dispatch_order_list.scak05`
  - 成品出库：`UB_ERP_Sales_order_list.xsak06`

## 主要接口

- `GET /api/stock-out/list`
- `GET /api/stock-out/:id`（列表展开、查看、编辑、打印共用；明细仅 `del=0`，含颜色名称拼接）
- `GET /api/stock-out/suggest-no`
- `GET /api/stock-out/warehouse-options`
- `GET /api/stock-out/list-related-party-options`
- `GET /api/stock-out/related-party-options`
- `GET /api/stock-out/material-options`
- `GET /api/stock-out/other-batch-lines`（其他出库批量选材分页）
- `POST /api/stock-out/other-batch-prices`（批量取最近复核入库价）
- `GET /api/stock-out/purchase-return-source-page`（采购退货关联采购单分页）
- `GET /api/stock-out/purchase-return-batch-lines`（采购退货批量添加明细）
- `GET /api/stock-out/assist-issue-source-page`（外协领料关联外协单分页）
- `GET /api/stock-out/production-dispatch-source-page`（生产领料关联派工单分页）
- `GET /api/stock-out/production-issue-batch-lines`（生产领料批量添加明细）
- `GET /api/stock-out/assist-issue-batch-lines`（外协领料批量添加明细）
- `GET /api/stock-out/inventory-summary`
- `GET /api/stock-out/source-lines`
- `GET /api/stock-out/print-data`
- `POST /api/stock-out`
- `PUT /api/stock-out/:id`
- `POST /api/stock-out/:id/audit`
- `POST /api/stock-out/:id/unaudit`
- `POST /api/stock-out/:id/restore`
- `DELETE /api/stock-out/:id`
- `DELETE /api/stock-out/:id/hard`

## SQL 兼容约束

本模块 SQL 必须兼容 SQL Server 2008 R2：

- 分页使用 `ROW_NUMBER()`。
- 不使用 `OFFSET`、`TRY_CONVERT`、`FORMAT`、`IIF`、`CONCAT`、`SEQUENCE`。

## 当前注意点

- 来源选择页第一版已保留接口入口，复杂的来源弹窗和 Excel 样式打印可继续单独增强。
- **其他出库批量选材**已上线；盘亏出库（类型 9）及其它类型的批量窗口仍待开发（生产领料批量添加已上线）。
- **厂款号/PI号保存**（2026-06-24）：明细 `Reference` 列写入须用物理列名 `Reference`（与 `tax`→`Tax` 同理），避免 `@reference` 未声明。
- **保存类型绑定**（2026-06-24）：主表 `kcap03/in_tax`、明细 `type/version/Tax/kcaq*` 等数值列须按 int/decimal 绑定参数，不能把字符串当 nvarchar 写入，否则会报 `Error converting data type nvarchar to numeric`。
- **明细保存统一口径**（2026-06-25）：保存时后端按明细行 `kcaa01` 查 `UB_ERP_Bom_000`，**`kcaq02`、`GUID`、`systemcode` 三列统一写 BOM.systemcode**（与旧系统一致；忽略前端批量带回的来源键）；`remark` 固定抄 `BOM.remark`；`kcaa02` 等物料快照仍抄 BOM 行。关联型出库「须来自关联单据」校验看 `sourceLineCode`/`lineKey`，不再看 `kcaq02`。
- 改后端后需要用户手动重启 API 服务，Agent 不自动重启。
