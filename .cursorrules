# ERP 项目内网开发 & 小白保姆指令集

## 1. 核心原则：听得懂、做得到
- **少术语、讲人话**：能不用术语就不用；不得不用时，必须用一句大白话解释清楚。
- **全中文沟通**：所有回复、代码注释、报错分析必须使用准确中文。
- **确认后再动手**：改“复杂逻辑/会影响数据/会影响很多文件”前，先说清楚改哪里、影响范围、回滚方式，你回复“OK”后再执行。

## 2. 开发者身份适配 (小白模式)
- **注释要有用**：不要求每一行都写；但凡是“业务规则/容易踩坑/为什么这么写”的地方，必须写中文注释说明意图。
- **手把手教操作**：如果你让我修改了数据库相关的代码，必须提醒我是否需要去 Navicat 检查表，或者是否需要重启终端。
- **代码示例先行**：每次写新功能，先给我演示一下这个功能的界面长什么样，不要直接甩一堆代码。

## 3. 内网环境 & 运维约束
- **数据库安全 (SQL Server)**：
    - 所有的 SQL 语句必须写在 `try-catch` 块里，如果报错了，要直接在网页上弹窗告诉我中文的错误原因。
    - 禁止在代码里硬编码数据库密码，必须强制引导我在 `.env` 文件里填写。
    - 字段类型必须选 `nvarchar`，防止我在 Navicat 里看到中文乱码。
    - **SQL Server 2008 R2 语法基线（强制）**：内网库按 **SQL Server 2008 R2** 为最低版本编写与审查，禁止默认假设 2012+。
        - **禁用或不得作为唯一路径的 2012+ 特性**（需改写或提供与 2008 等价的实现）：`TRY_CONVERT`、`TRY_CAST`、`FORMAT`、`IIF`、`CONCAT`（多参数内置函数）、`OFFSET ... FETCH`（分页）、`SEQUENCE` 等。
        - **安全类型转换（替代 TRY_CONVERT）**：用 `CASE` / `WHEN` 配合 `ISNUMERIC`（注意其边界）、`PATINDEX` 等先约束再 `CAST`/`CONVERT`；避免对可能非数字的字符串裸 `CAST`。
        - **典型报错链（排错顺序）**：若出现 `'TRY_CONVERT' is not a recognized built-in function name`，根因是 **语法超出 2008 R2**；同一批次里若紧跟 `Incorrect syntax near ')'`，多为其后解析 **连带错误**，应先消除版本不兼容语法再执行，不要只盯着括号改。
- **端口管理**：
    - 如果 3000 端口或 1433 端口被占用了，要教我怎么查进程并杀掉它。
    - **防旧进程指纹**：凡是我修改了后端接口/SQL/权限门禁后，必须提醒你做一次“后端重启确认”，并要求在后端启动日志打印一个可识别指纹（例如启动时间、git commit、或自增版本号）。没有指纹 = 不允许断言代码已生效。
- **内网兼容性**：
    - 考虑到内网可能无法随时下载新插件，尽量使用已经安装好的 Element Plus 和 Axios，不要随便乱加新的 npm 包。

## 4. 目录结构守则
- **严格归位**：必须按照 `src/views/[功能名]/index.vue` 这种物理目录来放文件，不许乱跑。
- **README 依赖**：每次进入一个新文件夹（比如 `inventory`），先读该目录下的 `README.md`，确认功能定义后再写代码。

## 5. 交互式调试 (Debug)
- **第 0 步：先排除“命中旧后端进程”**：只要你说“我刚改了后端/SQL/接口但页面没变化”、或我发现端口占用/多开服务迹象，必须先做下面自检，并在回复里给出结论与证据；未完成第 0 步前，不允许直接下 SQL/业务逻辑结论、更不允许让我继续改 SQL：
    1. 当前前端实际请求的 BaseURL/端口是多少（从浏览器 Network 或前端 axios baseURL 推断）。
    2. 该端口当前被哪个 PID 占用（给出 PID）。
    3. 后端日志中是否能看到“我刚加的标记日志/版本号/启动时间”（用它证明不是旧进程）。
    4. 若无法证明，默认结论为：页面仍命中旧进程 → 先指导重启/释放端口，再继续查权限/SQL/数据。
- **第一步诊脉**：报错时，先帮我分析是“我填错了”（前端）、“路不通”（网络/端口）、还是“仓库没货”（数据库没数据）。
- **给出修复方案**：不要只告诉我有错，直接给我一段可以直接复制粘贴的修复代码。

## 6. 自动化文档要求 (README 自动同步)
- **强制更新文档**：每当你完成一个功能（如：新增、修改、删除），必须自动检查并更新当前目录下的 `README.md`。
- **内容同步**：如果该目录下没有 `README.md`，请先根据当前代码逻辑创建一个。
- **记录内容**：
    1. 更新“已完成功能”列表。
    2. 如果涉及数据库改动，必须在 README 中记录新增的表字段或逻辑变化。
    3. 用“大白话”记录下这个功能目前存在的已知问题或下一步计划。
- **禁止偷懒**：不要只写“更新了代码”，要写清楚“增加了操作员保存功能，数据存入 Sys_Users 表”。

## 7. 模块标准化 (分页与列表)
- **默认分页**：所有包含数据表格的页面，必须默认实现分页功能（前端 el-pagination + 后端与 **SQL Server 2008 R2** 兼容的分页）。**默认**使用 `ROW_NUMBER() OVER (ORDER BY ...)` 外层筛选（或与 `server/index.js` 中已有「OFFSET 失败则降级 ROW_NUMBER」策略一致）；**不要**在 2008 R2 上把 `OFFSET-FETCH` 当作唯一实现（该语法自 2012 起才有）。
- **统一参数**：前端统一使用 `page` 和 `pageSize` 作为参数名，后端统一返回 `total` 和 `list`。
- **自定义容量**：所有分页组件必须包含 `page-sizes="[10, 20, 50, 100]"`，且默认为 20 条。
- **标准搜索栏**：
    - 位置：必须位于表格上方的独立卡片或区域。
    - 交互：输入框支持回车搜索，并配有“查询”和“重置”按钮。

## 8. 物理表接管与数据一致性 (Legacy Data Support)
- **接管模式**：本系统大量功能直接接管旧系统物理表（如 HR_Departments, HR_staff）。
- **字段锁定**：**禁止**私自修改、重命名或删除数据库已有字段。
- **状态位约定**：
    - `pass`: 审核状态。'1' 为已审核（锁定，不可删改），'0' 为未审核（草稿，可处理）。
    - `del`: 逻辑删除。'1' 为已删除，'0' 为正常。
- **参数对齐**：前后端传递审核状态时，统一使用 `pass` 作为键名，避免使用 `status` 或 `isAudited` 等混淆字段。

## 9. 权限拦截与调试自检 (Security & Debug)
- **权限门禁 (Gate)**：所有 `/api/*` 接口均受 `apiPermissionGate.js` 拦截。
- **401/403 诊断**：请求失败优先按下面顺序自检并给出结论：
    1. 前端是否正确携带了 `ERP_token`。
    2. 后端 Token 验证逻辑是否正常。
- **自检要求**：每次修改后端接口，必须确认 SQL 语句中已显式包含 `WHERE del = '0' AND pass = @pass`。

## 10. 页面逻辑特规 (Specific Logic)
- **初始视图**：进入页面默认**只查询** `pass = '1'` 的数据。
- **未审核切换**：界面必须提供“显示未审核”开关。开关开启时，查询 `pass = '0'` 的数据。
- **审核锁死**：前端模板中，凡是 `pass === '1'` 的行，必须通过 `:disabled` 属性禁用其“编辑”和“删除”按钮。

## 11. 项目技术栈速记（沟通用，少废话）
- **前端**：Vue 3（Composition API） + Element Plus + Vite + Axios（Bearer Token 拦截器）
- **后端**：Node.js + Express
- **认证**：JWT
- **权限**：自定义 `apiPermissionGate.js`（基于 Token 的按钮级权限校验）
- **数据库**：SQL Server **2008 R2**（内网；T-SQL 仅使用 **2008 R2 已支持** 的语法与函数，除非单独说明实例已升级）

## 12. MCP 工具（AI 自动判断什么时候该用）
- **总原则**：能不用就不用；需要用时，优先“只读/最小影响范围”。
- **必须先等你 OK 的情况**（有副作用）：
  - 可能改数据（执行 INSERT/UPDATE/DELETE/ALTER、跑迁移脚本）
  - 可能删文件/批量改很多文件/覆盖式改动
  - 可能启动/停止服务、结束进程、安装新依赖
  - 任何需要密码/Token 的操作（必须引导放到 `.env` 或系统环境变量；你贴密钥我必须提醒打码）
- **场景 → MCP 自动选择**：
  - 文件/代码/README/批量替换：`user-filesystem`
  - 跑命令/启动项目/脚本/查端口：`user-terminal`
  - 接口联调（401/403/没数据/参数错）：`user-http`
  - SQL Server 查表/查数据/迁移/验证 del-pass：`user-sqlserver`
  - 自动点页面回归/截图：`user-playwright`
  - PR/Issue/CI：`user-github`
  - 日志：`user-loki`；监控指标：`user-prometheus`；远程服务器：`user-ssh`
  - 记规则/读规则：`user-memory`
- **排错优先级（和第 5、9 条配套）**：
  - 改了后端但结果没变：先查“是否命中旧后端进程/端口漂移（第 5 条第 0 步）”→ 再查权限 Gate → 再查 SQL → 再查数据
  - 401：先查前端是否带 `ERP_token` → 再查后端 JWT 验证
  - 403：先查 `apiPermissionGate.js` 是否拦截 → 再查权限码/按钮权限
  - 列表没数据：先查 SQL 是否包含 `del='0' AND pass=@pass` → 再查数据是否被逻辑删除/未审核

## 13. 标准模块需求模板（你以后让我写功能就按这个问）
把下面这段当成“填空题”，你填完我就能一次性把前端、后端、SQL、README、回归流程对齐。
【模块名】模块（例如：部门管理）
### 页面需求（前端 Vue3 + Element Plus）
- 路径/菜单：`/xxx/xxx`
- 搜索条件（支持回车搜索）：（例如：名称、编码）
- 表格列：（按顺序列：字段名 + 中文名）
- 分页：`page=1,pageSize=20`；`page-sizes=[10,20,50,100]`
- 默认只查：`pass='1'` 且 `del='0'`
- 开关：显示未审核（开=查 `pass='0'`；关=查 `pass='1'`）
- 行内按钮：新增/编辑/删除/查看（你选）
- 审核锁死：当 `pass==='1'` 时，编辑/删除按钮必须禁用

### 接口需求（后端 Node/Express）
- 列表接口：`/api/xxx/list`
  - 入参：`page,pageSize,(搜索条件...),pass`
  - 返回：`{ total, list }`
  - SQL 必须包含：`WHERE del='0' AND pass=@pass`
  - 分页必须与 2008 R2 兼容：优先 `ROW_NUMBER()` 方案；若使用 `OFFSET-FETCH` 须带降级或与现有后端统一
- 新增接口：`/api/xxx/add`
- 编辑接口：`/api/xxx/update`
- 删除接口：逻辑删除 `del='1'`（并且 `pass='1'` 的数据禁止删）
- 权限 Gate：每个接口/按钮对应的权限码（你提供或让我给建议）

### 数据库（SQL Server）
- 版本假设：**2008 R2**（禁止默认可用 `TRY_CONVERT` / `OFFSET-FETCH` 等 2012+ 独占写法）
- 表：`xxx`
- 字段清单：只允许新增字段，禁止改/删旧字段；字段类型优先 `nvarchar`
- 迁移脚本：需要/不需要（你选）

### 文档（README）
- 必须更新当前目录 `README.md`：已完成功能 + 数据库变更 + 已知问题/下一步计划（大白话）

### 回归（Playwright）
- 流程：进入页面 → 搜索 → 翻页 → 新增 → 编辑 → 删除 → 切换“显示未审核” → 截图

## 14. CRUD 自动化闭环自检 (RPA 质检)
- **强制自检流程**：凡是涉及数据库新增（Create）、修改（Update）、删除（Delete）的功能，在交付代码前，必须执行以下流程：
    1. **启动自检**：调用 `user-playwright` 自动打开浏览器。
    2. **模拟操作**：AI 需根据刚才修改的 UI 逻辑，自动编写并执行一段 Playwright 脚本（模拟点击按钮、填入表单、点击提交）。
    3. **数据库核验**：操作完成后，必须调用 `user-sqlserver` 查询数据库，确认数据是否真的按照预期发生了变化（如：`code` 是否自增、`remark` 是否存入）。
    4. **截图凭证**：在对话框中展示自动化操作的关键步骤截图（Screenshot），证明功能已跑通。

- **异常处理**：如果 Playwright 模拟点击失败或 SQL 查询结果不符合预期，严禁交付代码，必须自行排查 bug 并在修复后重新跑通自检。
- **测试环境声明**：自动化测试仅限在本地 `localhost` 或指定的测试内网环境执行，禁止在未授权的情况下操作生产数据。

## 15. 数据库文档自动同步 (Self-Documenting)
- **同步触发条件**：凡是修改了后端 SQL 语句、增加了新表、或调整了 `INSERT/UPDATE` 字段逻辑，必须执行以下同步操作。
- **强制更新文档**：
    1. 修改完代码后，立即打开 `docs/database_map.md`。
    2. 根据最新的代码逻辑，更新对应的【业务功能】与【物理表】映射关系。
    3. 如果新增了字段（如 v1.1.0 增加的 `remark`），必须在文档的“备注/关键字段”一栏中补充。
- **自检逻辑**：更新文档前，AI 需调用 `user-sqlserver` 的 `get_table_schema` 确保文档记录的字段名与数据库真实情况完全一致。
- **禁止断更**：严禁出现代码已改但文档未同步的情况。如果被用户发现文档滞后，AI 必须道歉并立刻修正。
- **业务中文名 → 物理表名**：当用户提到“房间表/入住/电费”等中文业务名时，助手必须先查 docs/sql/database_map.md，用映射结果（例如 Hr_room）再继续写接口/SQL/页面字段。
- **缺失处理** ：如果映射表里没找到，助手必须先提示“映射缺失”，并给出建议把这一行补到 docs/sql/database_map.md（然后再继续）

## 16. 全局操作审计规范 (Global Audit Trail)
- **表结构强制要求**：
    1. **用户信息关联**：今后新建业务表（例如入库单 `Inv_StockIn`）必须默认包含：
        - `UID`: 对应 `Sys_Users.UserId`。
        - `uname`: 对应 `Sys_Users` 展示名（与登录态 `UserName`/`UserCode` 一致）。
    2. **时间戳**：必须包含 `create_time`（默认 `GETDATE()`）或 `update_time`。
    3. **存量表**：已存在的物理表（如 `HR_staff`）仍遵守「禁止私自改字段名」；仅在新增表时强制 UID/uname。
- **INSERT/UPDATE 写入操作者**：
    - `apiPermissionGate` 校验通过后会挂载 `req.user`（与 Bearer Token 解析结果一致）。
    - 执行 `INSERT`（及需要记录最后修改人时的 `UPDATE`）时，必须调用 `server/businessAuditFields.js` 的 `getActorAuditFromReq(req)` 得到 `{ UID, uname }` 并写入 SQL 参数，禁止在业务代码里手写重复取用户逻辑。
- **操作日志（Sys_OperationLogs）**：
    - **自动审计**：`POST`/`PUT`/`DELETE` 且 HTTP 200 时由 `operationAuditMiddleware` 按 `server/action_map.js` 映射写入；路径未在映射表命中时，`Action` 兜底为「系统操作」。
    - **手工补记**：关键分支或异步后续处理需额外落一条日志时，调用 `server/operationLogWriter.js` 的 `writeLog(req, action, details)`（自动带出 `UserId`/`UserName`/`IPAddress`；`CreateTime` 由表默认值填充）；`details` 可为字符串或可 JSON 的对象（对象会脱敏）。
    - 新业务接口上线前必须在 `action_map.js` 的 `OPERATION_AUDIT_ROUTE_RULES` 中补充 Method+Path 映射，避免长期落在「系统操作」。
- **日志表结构参考**：
    - `LogID`, `UserId`, `UserName`, `Action` (操作类型), `TargetTable` (目标表), `Content` (操作详情/SQL快照), `IPAddress`, `CreateTime`。
- **Playwright 自检**：自动化测试时，必须验证提交后 `Sys_OperationLogs` 是否生成了对应的审计记录。

## 17. 端口占用与进程清理硬约束 (Zombie Process Mitigation)
- **先破后立**：修改后端接口、SQL 或配置后，禁止直接重启。
- **端口自检**：必须先执行 `netstat -ano | findstr :3001` 查出 PID。
- **暴力清理**：发现占用必须执行 `taskkill /F /PID [PID]` 强制结束，确保旧进程彻底消失，防止“改了代码不生效”。
- **指纹验证**：重启后，后端日志必须打印包含“当前时间戳”或“版本号”的指纹信息（如：Server Reloaded at [Time]）。
- **失职判定**：若用户反馈改动无效且 AI 未执行 taskkill，视为 AI 失职。

### 18. 决策思考与逻辑确认协议 (Socratic Thinking Protocol)
- **核心目标**：防止财务逻辑漏洞，确保多表关联的原子性。
- **执行逻辑**：
    - **风险自评**：在处理涉及“金额计算”、“数据删除”、“跨表更新（如 Hr_staff 关联 Hr_room_in）”的操作前，必须先进行内部逻辑审计。
    - **强制确认**：若发现以下情况，必须以“> 【逻辑风险确认】”为开头向用户提问：
        1. 缺少边界处理：例如电费分摊未考虑“无人在住”或“优惠电量 > 总电量”的情况。
        2. 数据孤岛操作：例如删除了电费记录，但未同步清理或回滚关联的状态位。
        3. 性能隐患：在 SQL Server 2008 R2 环境下执行大表全表扫描。
- **防盲从机制**：如果用户的公式会导致逻辑悖论（如计算天数出现负数），必须指出并建议使用防御性函数（如 Math.max 或 SQL 的 CASE WHEN）。
- **效率平衡**：对于纯 UI 样式调整（CSS/HTML 布局）或单表简单查询，无需提问，直接执行。