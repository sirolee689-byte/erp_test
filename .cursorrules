# ERP 项目内网开发规则



## 0. 硬约束摘要（优先遵守）



- **认证**：前端 `localStorage` 键名为 **`erp_token`**（非 `ERP_token`）；请求头 `Authorization: Bearer <token>`。后端为 **内存 Map 随机 token**（重启失效），**不是 JWT**。

- **数据库**：最低 **SQL Server 2008 R2**；禁用 `TRY_CONVERT`、`OFFSET-FETCH` 等 2012+ 独占语法（细则见 §2）。

- **业务文档**：表/字段级映射与变更 → **仅维护** [`docs/sql/database_map.md`](docs/sql/database_map.md)；[`docs/database_map.md`](docs/database_map.md) 仅为入口跳转，**勿作为主编辑目标**。

- **接口调试**：凡怀疑「改了后端页面没变」→ 先做 **§4 第 0 步**（证明未命中旧 Node 进程），再查 SQL/权限。

- **后端/API 变更收尾**：凡修改 `server/**` 或影响接口的行为 → 必须按 **§14.1** 重启 **3001**、在回复中给出 **启动指纹**，并提醒 **重新登录**；**无指纹不得断言“已生效”**。

- **旧表**：禁止改/删已有字段；缺审计列以现存为准并在映射文档标注（见 §7 / §13）。



---



## 1. 交互与注释



1. **业务注释**：不要求逐行注释；但「业务规则/坑点/代码意图」必须有中文注释。

2. **全中文沟通**：所有回复、代码、报错分析必须使用准确中文。

3. **确认机制**：改动复杂逻辑前，必须说明影响范围及回滚方式，获 OK 后执行。



## 2. 内网环境 & 运维约束



- **数据库安全 (SQL Server)**：

    - 所有的 SQL 语句必须写在 `try-catch` 块里，如果报错了，要直接在网页上弹窗告诉我中文的错误原因。

    - 禁止在代码里硬编码数据库密码，必须强制引导我在 `.env` 文件里填写。

    - 字段类型必须选 `nvarchar`，防止我在 Navicat 里看到中文乱码。

    - **SQL Server 2008 R2 语法基线（强制）**：内网库按 **SQL Server 2008 R2** 为最低版本编写与审查，禁止默认假设 2012+。

        - **禁用或不得作为唯一路径的 2012+ 特性**（需改写或提供与 2008 等价的实现）：`TRY_CONVERT`、`TRY_CAST`、`FORMAT`、`IIF`、`CONCAT`（多参数内置函数）、`OFFSET ... FETCH`（分页）、`SEQUENCE` 等。

        - **安全类型转换（替代 TRY_CONVERT）**：用 `CASE` / `WHEN` 配合 `ISNUMERIC`（注意其边界）、`PATINDEX` 等先约束再 `CAST`/`CONVERT`；避免对可能非数字的字符串裸 `CAST`。

        - **典型报错链（排错顺序）**：若出现 `'TRY_CONVERT' is not a recognized built-in function name`，根因是 **语法超出 2008 R2**；同一批次里若紧跟 `Incorrect syntax near ')'`，多为其后解析 **连带错误**，应先消除版本不兼容语法再执行，不要只盯着括号改。

- **端口管理**：

    - 若 **3000 / 1433 / 3001（本仓库后端默认）** 被占用，说明如何查 PID 并结束进程；必要时可用 **`taskkill /F /IM node.exe /T`** 清理残留 Node（释放 **3001**，见 §17）。

    - **防旧进程指纹**：凡修改后端接口/SQL/权限门禁后，须提醒做一次「后端重启确认」，并要求启动日志打印可识别指纹（启动时间、git commit、或自增版本号）。**无指纹不得断言代码已生效**。

- **内网兼容性**：尽量使用已安装的 Element Plus 与 Axios，非必要不新增 npm 包。



## 3. 目录结构守则



- **严格归位**：按 `src/views/[功能名]/index.vue` 放置页面文件。

- **README 依赖**：进入新目录（如 `inventory`）先读该目录 `README.md`，确认功能定义后再写代码。



## 4. 交互式调试 (Debug)



- **第 0 步：先排除「命中旧后端进程」**：只要出现「刚改后端/SQL/接口但页面无变化」或端口占用/多开迹象，必须先完成下列自检并在回复中给出结论与证据；**未完成前不得直接改 SQL 或下业务结论**：

    1. 前端实际请求的 BaseURL/端口（Network 或 axios 推断）。

    2. 该端口占用 PID。

    3. 后端日志是否出现「新增标记/版本号/启动时间」（证明非旧进程）。

    4. 无法证明则默认仍命中旧进程 → 先重启/释放端口，再继续查权限/SQL/数据。

- **报错诊断**：区分「填错了」（前端）、「路不通」（网络/端口）、「库里没数据」（数据）。



## 5. 文档同步（README + 数据库映射）



完成功能（新增/修改/删除）后必须同步文档：



- **当前功能目录 `README.md`**（若无则创建）：

    - 更新「已完成功能」列表；涉及库表时在 README 中简述表/字段或逻辑变化（大白话）；记录已知问题与下一步。

    - 禁止只写「更新了代码」，须写清例如「增加操作员保存，写入 Sys_Users …」。

- **[`docs/sql/database_map.md`](docs/sql/database_map.md)**（业务中文名 ↔ 物理表、关键字段、迁移引用）：凡改后端 SQL、新表、或 `INSERT/UPDATE` 字段逻辑，必须更新此文件；更新前可用 `user-sqlserver` 的 `get_table_schema` 核对字段名。

- **业务用词**：提到「房间表/入住/电费」等中文业务名时，先查 `docs/sql/database_map.md` 得物理表名再继续；映射缺失则提示用户补充该行后再继续。



## 6. 模块标准化 (分页与列表)



- **默认分页**：表格页须分页（`el-pagination` + 与 **2008 R2** 兼容的后端分页）。默认 `ROW_NUMBER() OVER (ORDER BY …)` 外层筛选，或与 `server/index.js` 已有「OFFSET 不可用则降级 ROW_NUMBER」一致；**不得**仅以 `OFFSET-FETCH` 作为唯一实现。

- **统一参数**：前端 `page`、`pageSize`；后端返回 `total`、`list`。

- **分页 UI**：`page-sizes="[10, 20, 50, 100]"`，默认 **20**。

- **搜索栏**：独立卡片/区域；支持回车、「查询」「重置」。



## 7. 物理表接管与数据一致性 (Legacy Data Support)



- **接管模式**：大量功能直接接管旧系统物理表（如 HR_Departments、Hr_staff）。

- **字段锁定**：**禁止**私自修改、重命名或删除数据库已有字段。

- **状态位约定**：

    - `pass`：审核。`'1'` 已审核（锁定）；`'0'` 未审核。

    - `del`：逻辑删除。`'1'` 已删；`'0'` 正常。

    - **`Sys_Users.del`**：`'0'` 或空为启用可登录；`'1'` 禁用。登录与 `/api/users` 优先按 `del`；无该列再回退 `Status`。

- **参数对齐**：审核状态前后端统一键名 **`pass`**，避免 `status` / `isAudited` 混用。



## 8. 权限拦截与调试自检 (Security & Debug)



- **权限门禁**：`/api/*` 经 `apiPermissionGate.js` 拦截。

- **401/403 自检顺序**：

    1. 前端是否携带 **`erp_token`** 且请求头为 **`Authorization: Bearer <token>`**。

    2. 后端 token 校验是否正常（注意 **后端重启后内存 token 清空**）。

- **列表 SQL 原则**：涉及业务列表的查询须 **显式处理 `del`（通常排除已删）**；**`pass` 条件须与当前页面视图一致**（默认仅已审核 / 「显示未审核」开关等），禁止无意间查询全状态或遗漏 `del`。



## 9. 页面逻辑特规 (Specific Logic)



- **初始视图**：默认只查 **`pass = '1'`**。

- **未审核切换**：提供「显示未审核」开关；开启时查 **`pass = '0'`**。

- **审核锁死**：`pass === '1'` 的行禁用编辑、删除按钮（`:disabled`）。



## 10. 项目技术栈速记



- **前端**：Vue 3（Composition API）+ Element Plus + Vite + Axios（Bearer 拦截器，`erp_token`）。

- **后端**：Node.js + Express。

- **认证**：随机 token + **服务端内存存储**（非 JWT；重启需重新登录）。

- **权限**：`apiPermissionGate.js`（基于 Token 与菜单/按钮规则）。

- **数据库**：SQL Server **2008 R2**（T-SQL 仅用该版本已支持语法，除非单独说明实例已升级）。



## 11. MCP 与自动化质检



- **原则**：能 MCP 取证则少用空话。

- **场景**：页面交互/报错可用 browser-devtools MCP；字段与 `del`/`pass` 可用 `user-sqlserver`；端口/进程/重启可用 `user-terminal`；批量路径操作用 `user-filesystem`。

- **改代码后**：可主动询问是否需「浏览器实际操作 + SQL Server 核对数据」；路径示例：操作 → Console → 查库 → 对照 **§13** 审计描述规范。



## 12. 迁移脚本约定



- 数据库结构变更须有对应迁移：新增 `scripts/run-migration-sqlserver_v*.mjs`（或项目既有命名），并在根目录 **`package.json` 的 `migrate:*` 脚本** 中注册，便于内网复现。

- 只做「允许新增字段」类变更时仍须文档与 `docs/sql/database_map.md` 同步。



## 13. 全局操作审计规范 (Global Audit Trail)



- **审计字段**：**新建业务表**应规划 `uid, uname, utruename, addtime, edittime, deltime` 等（按模块惯例）；**旧表接管**以物理表现有列为准，缺列不得在规则层面要求擅自 `ALTER` 旧字段，须在 **`docs/sql/database_map.md`** 标注缺口。

- **禁止 JSON 裸投**：审计日志「详细内容」严禁整段无说明 JSON；须转为可读中文短句。

- **日志模版（面向用户）**：`[动作]了[模块名]，编号为：[code]，名称为：[name]。关键改动：[字段中文名]从[原值]变更为[新值]`。示例：`录入了供应商，编号为：SUP001，名称为：测试供应商。`

- **后端**：在构建 SQL 前拼接上述中文描述写入日志表。



## 14. 运维补充



### 14.1 后端 / API 变更强制协议（Agent 必须执行）

**目的**：避免用户在前端用 **旧 Node 进程（3001）** 造测试数据，误以为新代码未生效。

**触发条件（满足任一条即适用）**

- 修改 **`server/**`** 下任意文件（含 `index.js`、`apiPermissionGate.js`、中间件、审计、SQL 拼接等）。
- 修改 **影响 `node server/index.js` 启动或路由行为** 的根配置（若涉及）。
- 本会话任务涉及 **接口 / 保存 / 权限 / 路由 / 数据库写入**，且已对上述路径做过编辑。

**必须执行（环境允许时由 Agent 用终端完成；无法在沙箱执行时须给出你可复制执行的一键命令）**

1. **重启 Node API**：结束占用 **`.env` 中 `PORT`（默认 3001）** 的进程后，在项目根目录执行 **`npm run dev:server`**（等价：`node server/index.js`）。
2. **严禁误判**：本仓库 **`npm run dev` 仅启动 Vite（5173）**，**不会**启动 **3001**。不得仅运行 `npm run dev` 就声称「已重启后端」。接口相关验证 **以 3001 上为新进程为准**；若需前端热更新可同时保留 `npm run dev`，二者独立。
3. **回复中可核对证据（至少一项，缺一不可写「已生效」）**  
   - 后端控制台 **`[启动指纹]`** 或 **`bootAt=`** 时间戳；或  
   - 项目约定的启动标记行（如 **`BOM-SaveMain-Route-Active`** 等随版本增加的指纹）；或  
   - **`netstat -ano`**（或等价）显示 **3001 LISTENING** 及 **新 PID**。  
   **无证据 → 禁止**写「改好了可以测」「逻辑已生效」等断言，只能写「代码已保存，请你本地重启 API 后再验证」。
4. **必须提醒**：后端重启后 **内存 token 清空**，调用 `/api/*` 前需 **重新登录**（与 §0、§8 一致）。

**PowerShell**：串联命令使用 **`;`**，勿依赖 **`&&`**（部分环境会解析失败导致命令未执行）。

**杀进程策略**：优先 **`taskkill /PID <占用3001的PID> /F`**；仅在端口归属不明或用户明确要求时，使用下文 **`taskkill /F /IM node.exe /T`**（会结束本机 **全部** Node，慎用）。

---

- **杀 Node**：`taskkill /F /IM node.exe /T`（清理 **3001** 占用场景）。

- **环境隔离**：内网禁止外链 CDN，静态资源本地化。



## 15. 超长表格滚动优化



- **双滚动**：横向字段超过 **8** 列时，表格上方同步横向滚动或采用固定表头 + 固定左右列。

- **高度**：表格限制在视口内（如 `max-height="calc(100vh - 200px)"`），保证横向滚动条可见。

- **固定列**：**`s_code`（编码）**与 **操作列** 建议 `fixed`，避免横滑丢失关键信息。



## 16. 头表明细（主从）列表交互



凡页面为 **主表列表 + 行内展开加载明细**（`el-table-column type="expand"` 懒加载子表等）的模块，**统一按下列交互习惯**实现，后续新建或改版同类页面均须遵守；已有页面在改版触及主表表格区域时应顺带对齐。



1. **整行展开/收起**：用户可点击主表**数据行任意单元格**切换该行明细的展开与收起，**不得**仅依赖左侧 `>` 箭头作为唯一入口。

2. **实现要点（Element Plus）**：

    - 主表 `el-table` 绑定 **`ref`**；

    - 使用 **`@row-click`**，在回调内对当前行调用 **`toggleRowExpansion(row)`**（不传第二参数即为切换）；

    - 主表可增加专属 **`class`**（如 `xxx-main-table`），通过 `:deep(.el-table__body-wrapper .el-table__body tr) { cursor: pointer; }` 提示整行可点。

3. **排除误触（须在 `@row-click` 内显式 return）**：

    - 点击 **展开箭头**（如 `.el-table__expand-icon`）或 **`column?.type === 'expand'`** 时不再调用 `toggleRowExpansion`，避免与组件原生展开逻辑 **双重切换** 导致状态错乱；

    - 点击 **操作列** 内的 **`button` / `.el-button` / `a` / `input` / `textarea` / `select`** 时不触发展开，避免「点查看、编辑、审核」等按钮时误展开明细。

4. **明细加载**：展开时再请求明细接口的逻辑仍放在 **`@expand-change`**（或等价钩子）中，与「整行点击展开」并存；首次展开加载、缓存策略保持模块原有约定。




