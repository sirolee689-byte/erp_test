---
description: ERP 界面数值展示——四舍五入后去掉末尾无意义 0（数量/单价/金额通用）
alwaysApply: true
---

# ERP 数值展示（去末尾 0）

## 规则（展示层）

界面显示数量、单价、金额等**小数数值**时：

1. **先**按业务精度四舍五入（常见：数量 3 位、金额 2 位、单价 4 位）。
2. **再**去掉小数末尾无意义的 `0`；若小数部分全为 0，则去掉小数点。
3. **禁止**在界面上写死 `toFixed(3)` / `toFixed(2)` 后直接展示（会出现 `80.000`、`0.000`）。
4. 本规则只影响**展示**；落库、计算、校验仍用原有精度字段，不改业务含义。

## 示例

| 原值（精度） | 展示 |
|-------------|------|
| 80.000（3 位） | 80 |
| 54.540（3 位） | 54.54 |
| 0.000（3 位） | 0 |
| 100.00（2 位） | 100 |
| 0.5000（4 位） | 0.5 |

1 位、2 位、3 位、4 位小数**同一套规则**；位数由 `maxDecimals` 决定。

## 实现（单源）

- 工具：`src/utils/erpNumberDisplay.js`
- 通用：`formatErpTrimDecimal(value, { maxDecimals, empty })`
- 常用别名：
  - 数量 → `formatErpQtyDisplay`（3 位）
  - 金额 → `formatErpMoneyDisplay`（2 位）
  - 单价 → `formatErpPriceDisplay`（4 位）
- 计算对齐：`roundErpDecimal(value, maxDecimals)`

## 禁止

- 页面内再复制 `toFixed(n).replace(...)` 或各写一套 `formatNum`（除非迁移期且注明待替换）。
- 把展示字符串写回 v-model / 保存接口（展示与存储分离）。

## 空值

列表/表格无效数值默认显示 `-`（`ERP_NUMBER_DISPLAY_EMPTY`）；表单输入框可传 `empty: ''`。
