# CLAUDE

本文件供在本仓库工作的 AI 助手阅读；与项目约定相关的细节见 `docs/agents/`。

## Agent skills

### Issue tracker

工单以仓库内 `.scratch/<feature>/` 的 Markdown 文件跟踪。见 `docs/agents/issue-tracker.md`。

### Triage labels

分诊五类标签使用默认字符串（与角色名一致）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根目录 `CONTEXT.md` 与 `docs/adr/`。见 `docs/agents/domain.md`。

这是测试